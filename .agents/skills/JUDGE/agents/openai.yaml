interface:
  display_name: "JUDGE"
  short_description: "Repo-specific patterns and workflows for JUDGE"
  default_prompt: "Use the JUDGE repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true